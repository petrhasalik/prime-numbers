# 'use strict'

# describe 'Filter: toWords', ->

#   # load the filter's module
#   beforeEach module 'sampleAppApp'

#   # initialize a new instance of the filter before each test
#   toWords = undefined
#   beforeEach inject ($filter) ->
#     toWords = $filter 'toWords'

#   it 'should return the input prefixed with \'toWords filter:\'', ->
#     text = 'angularjs'
#     expect(toWords text).toBe 'toWords filter: ' + text
